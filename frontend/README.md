# Frontend

Flutter client application for Sami Shope.

This directory must contain frontend code only. It communicates with the backend through documented APIs and must not contain backend business logic or server secrets.
